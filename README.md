# AI_VISUALIZER
You must install version of the python allowing py game version
PY Game files must be installed if not install it throught the npm downloader
open file in vs code
Run code
Remember to first install the dependencies
